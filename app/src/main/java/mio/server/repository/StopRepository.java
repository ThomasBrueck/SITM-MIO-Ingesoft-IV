package mio.server.repository;

import mioice.Stop;

/**
 * Repositorio específico para Paradas
 */
public interface StopRepository extends IRepository<Stop, Integer> {
    // Métodos específicos si fueran necesarios, ej: findByName(String name)
}
